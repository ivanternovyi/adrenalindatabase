<div class="grid_16">
	<h1>Карточка учасника <%= "#{@user.user_detail.surname} #{@user.user_detail.name} #{@user.user_detail.mid_name}" %></h1>
</div>
<div class="clear"></div>
<div class="grid_16">
	<%= render 'form' %>
</div>
<p><%= link_to 'Back', :back %><%= link_to 'Delete', user_path(@user), method: :delete, data: {confirm: 'Are you sure?'} %></p>